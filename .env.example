# Hub API 베이스 URL. 실시간 메트릭 섹션(src/sections/LiveMetrics.tsx)이 이 값 +
# /v1/stats/public 을 호출한다. 비어 있으면 그 섹션은 fetch 자체를 시도하지 않고
# 조용히 사라진다 — 값을 못 구했다고 0이나 대시(-)를 보여주지 않는다.
# OPTiCS-Hub-Console 과 같은 변수명이다.
VITE_API_URL=http://localhost:3000
